services:
  app:
    build: .
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      DB_URL: "${DB_URL:?DB_URL is required}"
      DB_USERNAME: "${DB_USERNAME:?DB_USERNAME is required}"
      DB_PASSWORD: "${DB_PASSWORD:?DB_PASSWORD is required}"
      GOOGLE_CLIENT_ID: "${GOOGLE_CLIENT_ID:?GOOGLE_CLIENT_ID is required}"
      GOOGLE_CLIENT_SECRET: "${GOOGLE_CLIENT_SECRET:?GOOGLE_CLIENT_SECRET is required}"
      APP_ADMIN_SUBJECT: "${APP_ADMIN_SUBJECT:?APP_ADMIN_SUBJECT is required}"
      SITE_CONTACT_EMAIL: "${SITE_CONTACT_EMAIL:?SITE_CONTACT_EMAIL is required}"
      MIHOMO_USER_AGENT: "${MIHOMO_USER_AGENT:?MIHOMO_USER_AGENT is required}"
      ENKA_USER_AGENT: "${ENKA_USER_AGENT:?ENKA_USER_AGENT is required}"
      ENKA_CONNECT_TIMEOUT: "${ENKA_CONNECT_TIMEOUT:-3s}"
      ENKA_READ_TIMEOUT: "${ENKA_READ_TIMEOUT:-15s}"
      ENKA_CACHE_DEFAULT_TTL: "${ENKA_CACHE_DEFAULT_TTL:-60s}"
      ENKA_CACHE_MAXIMUM_TTL: "${ENKA_CACHE_MAXIMUM_TTL:-24h}"
      ENKA_CACHE_MAX_ENTRIES: "${ENKA_CACHE_MAX_ENTRIES:-5000}"
      ENKA_MAX_REQUESTS_PER_SECOND: "${ENKA_MAX_REQUESTS_PER_SECOND:-2}"
      ENKA_MAX_CONCURRENT_REQUESTS: "${ENKA_MAX_CONCURRENT_REQUESTS:-8}"
      ENKA_MAX_WAITING_REQUESTS: "${ENKA_MAX_WAITING_REQUESTS:-30}"
      ENKA_REQUEST_WAIT_TIMEOUT: "${ENKA_REQUEST_WAIT_TIMEOUT:-15s}"
      ENKA_BACKOFF_ON_429: "${ENKA_BACKOFF_ON_429:-30s}"
      TITLE_PRIVATE_UPLOAD_DIR: /app/private-uploads/title-verification
      TITLE_MAXIMUM_BYTES: "${TITLE_MAXIMUM_BYTES:-2097152}"
      TITLE_MAXIMUM_REQUEST_BYTES: "${TITLE_MAXIMUM_REQUEST_BYTES:-3145728}"
      TITLE_MAXIMUM_DIMENSION: "${TITLE_MAXIMUM_DIMENSION:-3200}"
      SPRING_THYMELEAF_CACHE: "${SPRING_THYMELEAF_CACHE:-true}"
      SERVER_FORWARD_HEADERS_STRATEGY: "${SERVER_FORWARD_HEADERS_STRATEGY:-framework}"
      SERVER_SERVLET_SESSION_COOKIE_SECURE: "${SERVER_SERVLET_SESSION_COOKIE_SECURE:-true}"
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - title-uploads:/app/private-uploads/title-verification
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - app
    environment:
      SITE_DOMAIN: "${SITE_DOMAIN:?SITE_DOMAIN is required}"
      TLS_CONTACT_EMAIL: "${TLS_CONTACT_EMAIL:?TLS_CONTACT_EMAIL is required}"
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  title-uploads:
  caddy-data:
  caddy-config:
