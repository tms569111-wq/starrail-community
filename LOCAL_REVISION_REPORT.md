# 로컬 수정 결과

이 폴더는 배포본이 아니라 JDK 21·MySQL 8 기반 로컬 실행본이다. 실제 OAuth·DB 비밀값은 포함하지 않았으며 `.env.example`과 `local-env.ps1.example`만 제공한다.

2026-08-18 추가 수정: 하나의 대형 V4를 기능별 V4~V9로 분리했다. 동일한 외래키 이름을 한 DDL에서 재사용하던 오류, 저장 생성열의 기반 컬럼에 `ON DELETE CASCADE`를 적용하던 MySQL 8.4 오류, 복수의 기존 버전을 모두 `OPEN`으로 이관하던 잠재 오류를 함께 수정했다. 최종 스키마 이름은 유지하므로 Java import 변경은 없다.

2026-08-18 화면·UID 추가 수정: 홈 대문을 두 줄 고정형 문구로 정리하고, 큰 카드 목록을 `T0`부터 `집계 대기`까지의 Prydwen형 티어 보드로 교체했다. MiHoMo V2 요청을 문서의 정식 파라미터로 맞췄으며, UID 자체 조회 실패와 전시 캐릭터 비공개를 서로 다른 상태 메시지로 분리했다.

## 반영한 7개 영역

1. **회원·계정**
   - Google의 안정적인 `sub` 식별, `APP_ADMIN_SUBJECT` 운영자 판정
   - Google 이름 비공개, 최초 닉네임 설정 전 모든 쓰기 차단
   - NFKC 정규화·예약어·유일성·30일 변경 대기·변경 이력
   - 활성/작성 정지/탈퇴 상태와 경고·1일·7일·30일·영구 제재
   - 탈퇴 시 개인정보·UID·증빙·추천·원시 투표 삭제와 댓글 익명 유지

2. **캐릭터 카탈로그**
   - 초기 90명 검색·속성/운명의 길 필터·현재 버전 Prydwen형 티어 행
   - 관리자 추가·수정·숨김·공개, 공급자별 외부 ID 유일성
   - 신규 캐릭터는 검토를 위해 숨김으로 생성하며 진행 중 버전의 명단을 깨지 않음

3. **UID·보유 인증**
   - 소개문 챌린지, UID 중복 연결 방지, 최고 성혼 누적
   - 별칭·보유 목록 일괄 조회로 N+1 제거
   - MiHoMo 우선/Enka 폴백, 3초 연결·8초 읽기 제한, 캐시·장애 회로
   - UID 미존재/조회 실패와 전시 캐릭터 비공개를 분리한 안내
   - 외부 HTTP와 DB 쓰기 트랜잭션 분리

4. **티어 투표·버전**
   - 고정 점수와 YAML 규칙/캐릭터별 문구 스냅샷
   - `DRAFT → OPEN → CLOSING → CLOSED`
   - 6개 성혼 필터 사전 집계와 최소 표본 표시
   - 버전 행 잠금으로 예약 집계·최종 종료 경합 방지
   - 종료 아카이브에 규칙, 전 필터 분포, 표 수, 평균, 티어, 집계 시각 저장

5. **댓글·답글·추천**
   - 최상위 댓글 20개 페이지와 한 단계 답글 전용 조회
   - 작성자 삭제/운영자 숨김 상태 분리, 삭제된 부모 자리표시자 유지
   - 본인 추천 금지, 중복 방지, 원자적 카운터
   - 회원 행 잠금으로 댓글 속도 제한과 탈퇴 경합 방지

6. **신고·운영 안전**
   - 댓글/답글 신고, 선택 상세 설명, 접수 내용 스냅샷
   - 본인·중복 신고 금지, UID 인증 필수, KST 기준 일일 10건
   - 신고만으로 자동 삭제하지 않고 숨김·제재를 독립 결정
   - 신고 결정 행 잠금과 복원 기능

7. **관리자·칭호·감사**
   - 버전·캐릭터·신고·칭호·회원·공급자 회로 대시보드
   - 직접 투표 수정, 직접 규칙 편집, 운영자 승격, 직접 칭호 부여 없음
   - JPG/PNG/WebP 700KB·1600px 제한, 재인코딩·메타데이터 제거·비공개 저장
   - 처리/만료 후 파일 삭제 재시도와 변경 불가 감사 기록

## 검증 상태

- Java 소스 구문 균형, 로컬 타입 import, YAML, HTML, JavaScript, CSS, SQL 정적 검사를 통과했다.
- H2 단위·웹 테스트와 Docker 사용 시 MySQL 8.4 Testcontainers 테스트를 추가했다. V3 레거시 데이터에서 V9까지의 이관과 핵심 데이터·제약조건도 별도로 검증한다.
- 현재 작업 환경에는 JDK 컴파일러/Gradle 배포본이 없고 외부 Gradle 다운로드가 차단되어 `gradlew test` 자체는 실행하지 못했다. 전달받은 환경에서 `./gradlew test` 또는 `gradlew.bat test`를 한 번 실행해야 한다.

## 의도적으로 제외한 것

공개 배포, Redis, 프록시/HTTPS 종단, 운영용 비밀 관리, 사용자 차단, 파티 추천, 개선 게시판은 이번 로컬 수정 범위에 포함하지 않았다.
